#include <stdio.h>

#include <stdio.h>

int main()
{
  

 
 
    int x,y,n,i,result;
    scanf("%d",&n);
     for(i=0;i<n;i++){
         
         scanf("%d%d", &x, &y);
         result=x+y;
         printf("%d\n",result);
     }

    return 0;
}